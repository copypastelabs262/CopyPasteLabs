# Learnings

Empty. One file per entry — see `../08_Templates/FORMATS.md` for the required format.

Before adding a file: grep this directory first. If a related entry exists, **update it**
rather than creating a second one. Then add the new file to `../INDEX.md`.
