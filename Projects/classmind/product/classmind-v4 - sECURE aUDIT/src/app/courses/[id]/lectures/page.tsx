import ClassLectures from "@/app/_components/shell/ClassLectures";

// The recordings: upload (owner) and the lecture index. Each lecture's detail
// page lives beneath this route, so the tab stays lit there.
export default function LecturesPage() {
  return <ClassLectures />;
}
