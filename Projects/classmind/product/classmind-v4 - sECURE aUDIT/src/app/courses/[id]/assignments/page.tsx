import ClassAssignments from "@/app/_components/shell/ClassAssignments";

// Work with a consequence gets its own destination: the owner's review queue
// and the class's confirmed assignments.
export default function AssignmentsPage() {
  return <ClassAssignments />;
}
